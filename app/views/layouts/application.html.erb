<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <nav>
      <ul>
        <li><a href="<%= blogs_path %>">Blogs</a></li>
        <li><a href="<%= new_blog_path %>">Create</a></li>
      </ul>
    </nav>
    <div id="main">
      <%= yield %>
    </div>
  </body>
</html>
